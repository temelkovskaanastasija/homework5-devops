name: Deploy Prod

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main' }}
    runs-on: [self-hosted, Windows, X64, devops]
    environment: prod

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull images
        run: |
          docker pull ghcr.io/${{ github.repository_owner }}/homework5-app:latest
          docker pull ghcr.io/${{ github.repository_owner }}/homework5-frontend:latest

      - name: Start prod environment
        run: |
          docker compose -p homework5-prod -f docker-compose.prod.yml down --remove-orphans
          docker compose -p homework5-prod -f docker-compose.prod.yml up -d

      - name: Check backend health
        shell: powershell
        run: |
          Start-Sleep -Seconds 5
          curl http://localhost:8083/health

      - name: Check frontend
        shell: powershell
        run: |
          curl http://localhost:3003